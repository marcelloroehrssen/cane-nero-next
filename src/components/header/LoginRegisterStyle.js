import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  row: {
    marginTop: theme.spacing(2)
  },
  facebookLogin: {marginTop: theme.spacing(2)},
  facebookLoginButton: {
    marginTop: theme.spacing(2),
    backgroundColor: '#3B5998'
  }
}))
